<view class="dish-card">
  <view class="thumb-wrap">
    <image
      wx:if="{{dish.image}}"
      class="thumb-image"
      src="{{dish.image}}"
      mode="aspectFit"
    />
    <text wx:else class="thumb-emoji">{{dish.emoji || '🍽️'}}</text>
  </view>

  <view class="dish-content">
    <text class="dish-title">{{dish.name}}</text>
    <text class="dish-desc">{{dish.desc}}</text>
  </view>

  <button
    class="action-btn {{actionVariant === 'danger' ? 'action-btn-danger' : ''}} {{disabled ? 'action-btn-disabled' : ''}}"
    disabled="{{disabled}}"
    bindtap="onTapAction"
  >
    {{disabled ? disabledText : actionText}}
  </button>
</view>
